pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'BRANCH 1'
                echo 'script test'
                sh "bash script.sh"
            }
        }
    }
}
